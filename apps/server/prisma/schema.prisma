// Prisma 骨架：只配 datasource / generator，本轮不建表、不迁移。
// 建表留待 B1 平台底座（配人工评审迁移，宪法：迁移脚本禁止 AI 直接生成合入）。

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

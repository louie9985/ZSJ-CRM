{
  "moduleOwner": "task-center",
  "purpose": "Add source-provided title and summary display snapshots while preserving lifecycle v1 replay with fixed neutral fallback text.",
  "applicationCompatibility": { "minimumInclusive": "0.0.0" },
  "lockImpact": "Adds two bounded text columns with constant defaults to the Task Center projection table.",
  "dataImpact": "Existing task facts and status are unchanged.",
  "backfill": "No source-module queries are performed. Existing rows retain fixed business-neutral fallback display text.",
  "recovery": "Retain the additive columns during application rollback and forward-fix consumers.",
  "forwardFix": "Append a new globally numbered migration. Never modify this migration after deployment.",
  "destructive": false
}

import {sql} from "drizzle-orm";
import {boolean,check,integer,jsonb,pgSchema,primaryKey,text,timestamp,uuid,varchar} from "drizzle-orm/pg-core";
const forms=pgSchema("form_schema");
export const drafts=forms.table("drafts",{definitionId:varchar("definition_id",{length:128}).primaryKey(),ownerModule:varchar("owner_module",{length:128}).notNull(),revision:integer("revision").notNull(),jsonSchema:jsonb("json_schema").notNull(),uiSchema:jsonb("ui_schema").notNull(),updatedAt:timestamp("updated_at",{mode:"string",withTimezone:true}).notNull()},t=>[check("form_draft_revision_positive",sql`${t.revision}>0`)]);
export const releases=forms.table("releases",{definitionId:varchar("definition_id",{length:128}).notNull(),releaseVersion:integer("release_version").notNull(),ownerModule:varchar("owner_module",{length:128}).notNull(),contentDigest:varchar("content_digest",{length:64}).notNull(),jsonSchema:jsonb("json_schema").notNull(),uiSchema:jsonb("ui_schema").notNull(),publishedAt:timestamp("published_at",{mode:"string",withTimezone:true}).notNull()},t=>[primaryKey({columns:[t.definitionId,t.releaseVersion]}),check("form_release_version_positive",sql`${t.releaseVersion}>0`),check("form_release_digest_length",sql`length(${t.contentDigest})=64`)]);
export const releaseStatus=forms.table("release_status",{definitionId:varchar("definition_id",{length:128}).notNull(),releaseVersion:integer("release_version").notNull(),active:boolean("active").notNull()},t=>[primaryKey({columns:[t.definitionId,t.releaseVersion]})]);
export const operationReceipts=forms.table("operation_receipts",{operationId:uuid("operation_id").primaryKey(),fingerprint:varchar("fingerprint",{length:64}).notNull(),result:jsonb("result").notNull()});
export const outboxEvents=forms.table("outbox_events",{eventId:uuid("event_id").primaryKey(),eventType:varchar("event_type",{length:64}).notNull(),payload:jsonb("payload").notNull(),occurredAt:timestamp("occurred_at",{mode:"string",withTimezone:true}).notNull(),publishedAt:timestamp("published_at",{mode:"string",withTimezone:true}),lastError:text("last_error")});

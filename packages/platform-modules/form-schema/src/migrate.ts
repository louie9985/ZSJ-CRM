import {readFile} from "node:fs/promises";import {resolve} from "node:path";import {runMigrations} from "@ai-crm/database";const path=process.env.DATABASE_MIGRATION_URL_FILE;if(!path)throw new Error("DATABASE_MIGRATION_URL_FILE is required.");const connection=(await readFile(resolve(path),"utf8")).trim();if(!connection)throw new Error("DATABASE_MIGRATION_URL_FILE resolved to an empty Secret.");await runMigrations(connection,resolve(import.meta.dirname,"../migrations"));

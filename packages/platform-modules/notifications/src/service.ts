/* eslint-disable @typescript-eslint/no-unnecessary-condition,@typescript-eslint/restrict-template-expressions -- runtime ports and numeric contract versions are validated at this boundary. */
import { createHash } from "node:crypto";
import { NotificationError } from "./errors.js";
import { digestTemplate,renderTemplate,validateTemplateRelease,validateTemplateSource } from "./template.js";
import { actor,cursor,deepLink,id,jsonRecord,recipient,selector,sha256,timestamp,uuid } from "./validation.js";
import type { InAppNotification,NotificationActor,NotificationAudit,NotificationAuthorization,NotificationCenter,NotificationIntent,NotificationObserver,NotificationOperation,NotificationPreference,NotificationRecipientResolver,NotificationStore,PublishTemplateCommand,ResolvedRecipient,TemplateRelease } from "./types.js";
const notificationUuid=(intentId:string,principalId:string,recipientReference:string):string=>{const hex=createHash("sha256").update(`${intentId}\u0000${principalId}\u0000${recipientReference}`).digest("hex").slice(0,32).split("");hex[12]="5";hex[16]=((Number.parseInt(hex[16]??"0",16)&3)|8).toString(16);return`${hex.slice(0,8).join("")}-${hex.slice(8,12).join("")}-${hex.slice(12,16).join("")}-${hex.slice(16,20).join("")}-${hex.slice(20).join("")}`;};
const mapped=(error:unknown,fallback:"NOTIFICATION_RECIPIENT_RESOLUTION_FAILED"|"NOTIFICATION_STORAGE_UNAVAILABLE"):NotificationError=>error instanceof NotificationError?error:new NotificationError(fallback,{cause:error,retryable:true});
const deduplicateRecipients=(resolved:readonly ResolvedRecipient[]):readonly ResolvedRecipient[]=>{const targets=new Map<string,ResolvedRecipient>();for(const item of resolved){const key=`${item.principalId}\u0000${item.recipientReference}`;const existing=targets.get(key);if(existing&&(existing.resolutionReference!==item.resolutionReference||existing.resolutionVersion!==item.resolutionVersion))throw new NotificationError("NOTIFICATION_RECIPIENT_RESOLUTION_FAILED");targets.set(key,item);}return[...targets.values()];};
export const createNotificationCenter=(ports:{readonly store:NotificationStore;readonly authorization:NotificationAuthorization;readonly audit:NotificationAudit;readonly resolver:NotificationRecipientResolver;readonly preference:NotificationPreference;readonly observer?:NotificationObserver;readonly now?:()=>Date}):NotificationCenter=>{
  const clock=ports.now??(()=>new Date());
  const recordObservation=(input:Parameters<NotificationObserver["record"]>[0]):void=>{try{ports.observer?.record(input);}catch{/* Technical telemetry is non-authoritative and must never change the business result. */}};
  const observe=async<T>(operation:NotificationOperation,work:()=>Promise<T>):Promise<T>=>{const started=Date.now();try{const result=await work();recordObservation({operation,outcome:"completed",durationMs:Math.max(0,Date.now()-started)});return result;}catch(error){recordObservation({operation,outcome:error instanceof NotificationError&&error.code==="NOTIFICATION_OPERATION_DENIED"?"denied":"failed",durationMs:Math.max(0,Date.now()-started)});throw error;}};
  const audit=async(input:Parameters<NotificationAudit["record"]>[0]):Promise<void>=>{try{await ports.audit.record(input);}catch(error){throw new NotificationError("NOTIFICATION_AUDIT_FAILED",{cause:error,retryable:true});}};
  const authorize=async(actorInput:NotificationActor,operation:NotificationOperation,referenceId:string,extra?:{notificationId?:string;ownerReference?:string;producerReference?:string}):Promise<{actor:NotificationActor;decisionId:string}>=>{const validActor=actor(actorInput);let decision;try{decision=await ports.authorization.authorize({actor:validActor,operation,...extra});}catch(error){throw new NotificationError("NOTIFICATION_AUTHORIZATION_FAILED",{cause:error,retryable:true});}const decisionId=id(decision.decisionId);if(!Object.is(decision.allowed,true)){await audit({actor:validActor,operation,phase:"failed",decisionId,referenceId,errorCode:"NOTIFICATION_OPERATION_DENIED"});throw new NotificationError("NOTIFICATION_OPERATION_DENIED");}await audit({actor:validActor,operation,phase:"attempted",decisionId,referenceId});return{actor:validActor,decisionId};};
  const finish=(context:{actor:NotificationActor;decisionId:string},operation:NotificationOperation,referenceId:string,error?:NotificationError):Promise<void>=>audit({actor:context.actor,operation,phase:error===undefined?"succeeded":"failed",decisionId:context.decisionId,referenceId,...(error?{errorCode:error.code}:{})});
  const read=async(actorInput:NotificationActor,notificationIdInput:string,operation:NotificationOperation):Promise<{item:InAppNotification;context:{actor:NotificationActor;decisionId:string}}>=>{const notificationId=uuid(notificationIdInput);const context=await authorize(actorInput,operation,notificationId,{notificationId});try{const item=await ports.store.get(context.actor.principalId,notificationId);if(!item)throw new NotificationError("NOTIFICATION_NOT_FOUND");return{item,context};}catch(error){const failure=mapped(error,"NOTIFICATION_STORAGE_UNAVAILABLE");await finish(context,operation,notificationId,failure);throw failure;}};
  return{
    publishTemplate:(command:PublishTemplateCommand)=>observe("notification_template_publish",async()=>{const validActor=actor(command.actor);const templateKey=id(command.templateKey);const ownerReference=id(command.ownerReference);const context=await authorize(validActor,"notification_template_publish",`${templateKey}:${command.version}`,{ownerReference});try{if(!Number.isSafeInteger(command.version)||command.version<1)throw new NotificationError("NOTIFICATION_INPUT_INVALID");const base={templateKey,version:command.version,ownerReference,notificationType:id(command.notificationType),variableSchema:structuredClone(command.variableSchema),titleTemplate:validateTemplateSource(command.titleTemplate),bodyTemplate:validateTemplateSource(command.bodyTemplate),publishedAt:timestamp(command.publishedAt)};const release:TemplateRelease={...base,contentDigest:digestTemplate(base)};validateTemplateRelease(release);await ports.store.publishTemplate(release);await finish(context,"notification_template_publish",`${templateKey}:${command.version}`);return release;}catch(error){const failure=mapped(error,"NOTIFICATION_STORAGE_UNAVAILABLE");await finish(context,"notification_template_publish",`${templateKey}:${command.version}`,failure);throw failure;}}),
    submitIntent:(actorInput:NotificationActor,input:NotificationIntent)=>observe("notification_intent_submit",async()=>{const intentId=uuid(input.intentId);const producer=id(input.producer);const idempotencyKey=id(input.idempotencyKey);const context=await authorize(actorInput,"notification_intent_submit",intentId,{producerReference:producer});try{if(!Number.isSafeInteger(input.templateVersion)||input.templateVersion<1||input.selectors.length<1||input.selectors.length>100)throw new NotificationError("NOTIFICATION_INPUT_INVALID");const variables=jsonRecord(input.variables);const normalized={intentId,producer,idempotencyKey,templateKey:id(input.templateKey),templateVersion:input.templateVersion,selectors:input.selectors.map(selector),variables,sourceType:id(input.sourceType),sourceId:id(input.sourceId),deepLink:deepLink(input.deepLink)};const requestFingerprint=sha256(normalized);const existing=await ports.store.findIntent(producer,idempotencyKey);if(existing){if(existing.fingerprint!==requestFingerprint)throw new NotificationError("NOTIFICATION_CONFLICT");await finish(context,"notification_intent_submit",intentId);return existing.result;}const release=await ports.store.getTemplate(normalized.templateKey,normalized.templateVersion);if(!release)throw new NotificationError("NOTIFICATION_TEMPLATE_NOT_FOUND");const rendered=renderTemplate(release,variables);let resolved:readonly ResolvedRecipient[];try{resolved=(await ports.resolver.resolve(normalized.selectors)).map(recipient);}catch(error){throw mapped(error,"NOTIFICATION_RECIPIENT_RESOLUTION_FAILED");}const recipients=deduplicateRecipients(resolved);if(recipients.length<1||recipients.length>100)throw new NotificationError("NOTIFICATION_RECIPIENT_RESOLUTION_FAILED");const createdAt=timestamp(clock().toISOString());const notifications:InAppNotification[]=[];for(const target of recipients){const decision=await ports.preference.evaluate({notificationType:release.notificationType,recipient:target});if(decision.decision!=="deliver"&&decision.decision!=="suppress")throw new NotificationError("NOTIFICATION_INPUT_INVALID");notifications.push({notificationId:notificationUuid(intentId,target.principalId,target.recipientReference),intentId,principalId:target.principalId,recipientReference:target.recipientReference,resolutionReference:target.resolutionReference,resolutionVersion:target.resolutionVersion,templateKey:release.templateKey,templateVersion:release.version,notificationType:release.notificationType,title:rendered.title,body:rendered.body,sourceType:normalized.sourceType,sourceId:normalized.sourceId,deepLink:normalized.deepLink,preferenceDecision:decision.decision,preferenceReason:id(decision.reason),preferenceVersion:id(decision.version),createdAt});}const result={intentId,notificationIds:notifications.map(item=>item.notificationId),status:"accepted" as const};const accepted=await ports.store.acceptIntent({intent:{intentId,producer,idempotencyKey,fingerprint:requestFingerprint,result,createdAt},notifications});await finish(context,"notification_intent_submit",intentId);return accepted.result;}catch(error){const failure=mapped(error,"NOTIFICATION_STORAGE_UNAVAILABLE");await finish(context,"notification_intent_submit",intentId,failure);throw failure;}}),
    get:(actorInput,notificationId)=>observe("notification_detail",async()=>{const result=await read(actorInput,notificationId,"notification_detail");await finish(result.context,"notification_detail",result.item.notificationId);return result.item;}),
    list:(query)=>observe("notification_list",async()=>{const context=await authorize(query.actor,"notification_list",query.actor.principalId);try{const limit=query.limit??50;if(!Number.isSafeInteger(limit)||limit<1||limit>100)throw new NotificationError("NOTIFICATION_INPUT_INVALID");const page=await ports.store.list({principalId:context.actor.principalId,limit,includeArchived:query.includeArchived??false,...(query.cursor===undefined?{}:{cursor:cursor(query.cursor)})});await finish(context,"notification_list",context.actor.principalId);return page;}catch(error){const failure=mapped(error,"NOTIFICATION_STORAGE_UNAVAILABLE");await finish(context,"notification_list",context.actor.principalId,failure);throw failure;}}),
    unreadCount:(actorInput)=>observe("notification_unread_count",async()=>{const context=await authorize(actorInput,"notification_unread_count",actorInput.principalId);try{const count=await ports.store.unreadCount(context.actor.principalId);await finish(context,"notification_unread_count",context.actor.principalId);return count;}catch(error){const failure=mapped(error,"NOTIFICATION_STORAGE_UNAVAILABLE");await finish(context,"notification_unread_count",context.actor.principalId,failure);throw failure;}}),
    markRead:(command)=>observe("notification_mark_read",async()=>{const found=await read(command.actor,command.notificationId,"notification_mark_read");try{const item=await ports.store.markRead(found.context.actor.principalId,found.item.notificationId,timestamp(clock().toISOString()));if(!item)throw new NotificationError("NOTIFICATION_NOT_FOUND");await finish(found.context,"notification_mark_read",found.item.notificationId);return item;}catch(error){const failure=mapped(error,"NOTIFICATION_STORAGE_UNAVAILABLE");await finish(found.context,"notification_mark_read",found.item.notificationId,failure);throw failure;}}),
    archive:(command)=>observe("notification_archive",async()=>{const found=await read(command.actor,command.notificationId,"notification_archive");try{const item=await ports.store.archive(found.context.actor.principalId,found.item.notificationId,timestamp(clock().toISOString()));if(!item)throw new NotificationError("NOTIFICATION_NOT_FOUND");await finish(found.context,"notification_archive",found.item.notificationId);return item;}catch(error){const failure=mapped(error,"NOTIFICATION_STORAGE_UNAVAILABLE");await finish(found.context,"notification_archive",found.item.notificationId,failure);throw failure;}})
  };
};

import { spawnSync } from "node:child_process";
import { randomUUID } from "node:crypto";
import { mkdtemp,rm } from "node:fs/promises";
import { createServer } from "node:net";
import { tmpdir } from "node:os";
import { resolve } from "node:path";
import { setTimeout as delay } from "node:timers/promises";
const port=Number(process.env.AI_CRM_TEST_NOTIFICATIONS_POSTGRES_PORT??"55439");if(!Number.isSafeInteger(port)||port<1024||port>65_535)throw new Error("Invalid Notifications integration test port.");
await new Promise((ready,reject)=>{const server=createServer();server.once("error",()=>reject(new Error(`Integration test port ${port} is unavailable.`)));server.listen(port,"127.0.0.1",()=>server.close(ready));});
const pnpmCli=process.env.npm_execpath;if(!pnpmCli)throw new Error("pnpm CLI path is unavailable.");const secretDirectory=await mkdtemp(resolve(tmpdir(),"ai-crm-prc03-"));const project=`ai-crm-test-prc03-${randomUUID().slice(0,8)}`;
const environment={...process.env,AI_CRM_COMPOSE_SECRET_DIR:secretDirectory,AI_CRM_CREATE_TEST_MIGRATION_URL:"1",AI_CRM_TEST_POSTGRES_PORT:String(port),DATABASE_MIGRATION_URL_FILE:resolve(secretDirectory,"migration_url"),TEST_NOTIFICATIONS_DATABASE_URL_FILE:resolve(secretDirectory,"migration_url")};
const compose=["compose","-p",project,"-f","deploy/compose/compose.base.yml","-f","deploy/compose/compose.postgres-test.yml"];const run=(command,args)=>{const result=spawnSync(command,args,{cwd:resolve(import.meta.dirname,"../../../.."),env:environment,shell:false,stdio:"inherit"});if(result.status!==0)throw new Error(`${command} ${args[0]??""} failed.`);};
const waitForStablePostgres=async()=>{const deadline=Date.now()+30_000;let consecutiveReady=0;while(Date.now()<deadline){const probe=spawnSync("docker",[...compose,"exec","-T","postgres","pg_isready","--host","127.0.0.1","--username","ai_crm_admin","--dbname","postgres"],{cwd:resolve(import.meta.dirname,"../../../.."),encoding:"utf8",env:environment,shell:false,stdio:["ignore","ignore","ignore"],timeout:2_000});consecutiveReady=!probe.error&&probe.status===0?consecutiveReady+1:0;if(consecutiveReady>=2)return;await delay(250);}throw new Error("Isolated PostgreSQL did not become stably reachable over TCP.");};
let primaryFailure;try{run(process.execPath,["scripts/bootstrap/compose-secrets.mjs","test"]);run("docker",[...compose,"up","-d","--wait","postgres"]);await waitForStablePostgres();run(process.execPath,[pnpmCli,"db:migrate"]);run(process.execPath,[pnpmCli,"--filter","@ai-crm/platform-notifications","exec","vitest","run","--config","../../../vitest.config.ts","src/postgres-store.integration.test.ts"]);}catch(error){primaryFailure=error;}
const cleanupFailures=[];const composeCleanup=spawnSync("docker",[...compose,"down","--volumes","--remove-orphans"],{cwd:resolve(import.meta.dirname,"../../../.."),env:environment,shell:false,stdio:"inherit"});if(composeCleanup.error||composeCleanup.status!==0){cleanupFailures.push(new Error(`Notifications integration Compose cleanup failed; temporary Secret directory retained at ${secretDirectory}.`));}else{try{await rm(secretDirectory,{force:true,recursive:true});}catch(error){cleanupFailures.push(new Error(`Notifications integration Secret cleanup failed; manual cleanup is required at ${secretDirectory}.`,{cause:error}));}}if(primaryFailure||cleanupFailures.length>0)throw new AggregateError([...(primaryFailure?[primaryFailure]:[]),...cleanupFailures],"Notifications integration run or cleanup failed.");

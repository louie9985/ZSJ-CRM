{
  "name": "@ai-crm/crm-sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": { ".": "./dist/index.js" },
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "pnpm --filter @ai-crm/crm-authorization build && tsc -p tsconfig.json --noEmit --composite false",
    "test": "vitest run --config ../../vitest.config.ts",
    "contracts:check": "node ../../scripts/check/verify-package.mjs"
  },
  "dependencies": {
    "@ai-crm/crm-authorization": "workspace:*"
  }
}

# 第一阶段多线并行执行总表

- 状态：执行中
- 更新日期：2026-08-02
- 适用范围：第一阶段公共技术底座、业务中立 Walking Skeleton 的本地多 Agent 并行实施
- 权威实施基线：[第一阶段 AI 并行开发实施计划](./第一阶段AI并行开发实施计划.md)
- 验收基线：[第一阶段 Walking Skeleton 验收清单](../06-质量验收/第一阶段Walking-Skeleton验收清单.md)

## 1. 用途与边界

本表只记录工作包状态、所有权、Review 闭环、共享资源租约、合并门和验收证据，不承载永久业务规则，也不替代根 `AGENTS.md`、ADR、契约、模块说明、实施计划或验收清单。

已知事实：

- G1 已完成。
- `IAM-01`～`IAM-03`、`ASY-01`、`PRC-01` 已通过 G2。
- 全部模块已通过 G2；`CMP-01` 的仓库代码与发布门已随 `e090dda` 合并。授权发布治理/管理权限目录/基线编译、API Task/Notification/File 查询与 COS Ready、独立 API/Worker 数据库角色、Task Outbox/Inbox/投影 Worker、AMQPS 固定重试/DLQ、告警/恢复合同及应用镜像流水线已闭环。真实首发策略、COS test Bucket、镜像摘要、生产 TLS/CAM/告警/恢复证据和显式消费者启用仍是外部发布阻断；Notification/Workflow/File Job 因缺少已审合同不创建消费者。
- `CLI-01` 已完成业务中立化整改、独立代码与四视口视觉 Review，并串行合并。
- 本地已配置 `origin`，`origin/main` 包含基线 `9fa4e2c`，历史 CI 与应用镜像工作流证据已记录；2026-08-02 本轮实时读取 GitHub 时本地代理连接失败，未取得当前候选的新远端结果。服务端 required checks、审批、CODEOWNER enforcement、force-push/删除保护仍未闭合。

允许的假设：

- 使用合成人员、组织、权限、任务、通知、表单、文件、流程和故障 Fixture 验证平台能力。
- 四个 Agent 仅在互相隔离的本地 Git Worktree 中并行写入。
- `main` 由 Integration Owner 串行更新，本地流程控制暂时代替尚不存在的远程保护。

禁止的假设：

- 不从 Demo、访谈或历史材料推断 CRM 实体、字段、状态、角色、SLA、审批路线或指标。
- 不把本地 Worktree 隔离描述为远程主分支保护或完整 G0 技术门禁。
- 不允许多个 Owner 同时修改根 Lockfile、生成制品、全局迁移编号或 Composition Root。

非目标：

- 本表不新增 CRM 领域模块、真实 Provider、真实 AI 用途或生产 Secret。
- 本表不承诺日历排期、人员工时、SLA、RPO、RTO 或自动故障切换。

## 2. 状态机与完成规则

工作包使用以下状态：

```text
BACKLOG -> READY -> IMPLEMENTING -> SELF_REVIEW -> INDEPENDENT_REVIEW
  -> FIXING -> RE_REVIEW -> G2_ACCEPTED -> MERGED
```

任何阶段均可进入 `BLOCKED`。`EVIDENCE_BLOCKED` 表示授权范围内的仓库代码和发布门已经合并，但必须由受保护环境产生的外部证据尚未闭合；它不等同于仍在实现，也不等同于验收通过。只有专项测试、完整 `pnpm check` 和独立 Review 均通过，且所有可执行 P0/P1/P2/P3 finding 清零、未决架构或契约问题清零，才可进入 `G2_ACCEPTED`。Review 结论必须写入任务 handoff，不能只写 `LGTM`。

## 3. 四线总览

| 开发线 | 顺序 | Owner | 独立 Reviewer | 分支/Worktree 规则 | 当前状态 |
|---|---|---|---|---|---|
| L1 流程协同 | `PRC-02 -> PRC-03` | Agent A | Agent C | `task/<task-id>-<slug>` / `D:\\AI-CRM-worktrees\\<task-id>` | PRC-02、PRC-03 已合并，开发线完成 |
| L2 平台控制面 | `PLT-01 -> PLT-02 -> PLT-03` | Agent B | Agent D | 同上 | PLT-01、PLT-02、PLT-03 已合并，开发线完成 |
| L3 多端客户端 | `CLI-01 -> CLI-02 -> CLI-03` | Agent C | Agent A | 同上 | CLI-01～CLI-03 已合并并通过独立 Review，开发线完成 |
| L4 韧性与 AI | `INT-01 -> AIG-01 -> OPS-01` | Agent D | Agent B | 同上 | INT-01、AIG-01、OPS-01 已合并，开发线完成 |

Agent D 在 L4 完成后转为 Integration Owner。四线模块 G2 完成后，串行执行 `CMP-01 -> E2E-01 -> OPS-02`。

## 4. 工作包执行表

| 工作包 | Owner | Reviewer | 允许路径摘要 | 依赖 | 状态 | Review 轮次 | 未关闭问题 | 合并提交 |
|---|---|---|---|---|---|---:|---:|---|
| PRC-02 | Agent A | Agent C | Task Center、任务契约、模块迁移 | G1、PRC-01、ASY-01 | MERGED | 2 | 0 | `c39fce6` |
| PRC-03 | Agent A | Agent C | Notifications、通知契约、模块迁移 | PRC-02 G2 | MERGED | 3 | 0 | `055c4ac` |
| PLT-01 | Agent B | Agent D | Audit、App Registry、对应契约与迁移 | G1、IAM-03 | MERGED | 2 | 0 | `0c816d3` |
| PLT-02 | Agent B | Agent D | Form Schema、Business Configuration、契约与迁移 | PLT-01 G2 | MERGED | 2 | 0 | `f2447ad` |
| PLT-03 | Agent B | Agent D | File Center、文件契约与迁移 | PLT-02 G2 | MERGED | 2 | 0 | `4e5ffd3` |
| CLI-01 | Agent C | Agent A | Workbench Web、必要的 Shared UI | 相关契约 G2 | MERGED | 4 | 0 | `e4d76e7` |
| CLI-02 | Agent C | Agent A | Internal Mobile | 相关契约 G2、CLI-01 模式稳定 | MERGED | 6 | 0 | `3a853c0` |
| CLI-03 | Agent C | Agent A | External Portal、外部 Allowlist Client 配置 | 外部契约与 Client 稳定 | MERGED | 2 | 0 | `87666f6` |
| INT-01 | Agent D | Agent B | Integration Runtime、集成契约、故障工具 | G1 | MERGED | 2 | 0 | `843c6b7` |
| AIG-01 | Agent D | Agent B | AI Gateway、AI 契约、合成 Fixture | INT-01 G2 | MERGED | 3 | 0 | `119b94c` |
| OPS-01 | Agent D | Agent B | Deploy、发布文档与运维检查 | 运行组件接口稳定 | MERGED | 3 | 0 | `23cc467` |
| CMP-01 | Agent D | Agent B | API/Worker Composition Root | 所需模块 G2 | EVIDENCE_BLOCKED | 17 | 5 项外部证据类 | `e090dda` |
| E2E-01 | Agent A | Agent C | `tests/e2e`、测试 Fixture | G3 | MERGED | 多轮 | 0 | `a7c3e90` |
| OPS-02 | Agent D | Agent A | 恢复、安全演练与证据 | G4 | EXTERNAL_BLOCKED | 1 | 缺受控环境、故障域外备份目标、正式操作/复核身份与真实恢复演练 | 仓库基础已合并 |

## 5. 共享资源租约

| 共享资源 | 单一 Owner | 当前租约 | 规则 |
|---|---|---|---|
| `pnpm-lock.yaml` | Integration Owner | 已纳入 Worker `amqplib@2.0.1`、Worker/authorization 的 `@ai-crm/database workspace:*`，frozen install 和最新主线 `pnpm check` 140/140 通过；当前未出租 | 开发线不提交 Lockfile；合并窗口统一更新并 frozen 验证 |
| Generated OpenAPI/API Client/Manifest | Integration Owner | CMP-01 权限合同生成窗口已完成 | 契约源先评审；单一生成窗口更新并与源契约一同合并 |
| 全局迁移编号 | Integration Owner | `0000000004`～`0000000010` 已随模块工作包登记；`0000000011` 为迁移兼容证据，`0000000012` 为授权持久化，`0000000013` 为 API 运行角色权限，`0000000014` 为 Task Projection Worker 权限；最终隔离 PostgreSQL 39/39 通过 | 新迁移写入前登记；不得复用或修改已部署迁移 |
| `apps/api`、`apps/worker` | CMP-01 Owner | G3 仓库侧组合已随 `e090dda` 合并；当前仅允许受审证据闭环或新合同驱动的后续变更 | 缺少已审合同不得新增 Notification、Workflow、File 或通用 Job 消费者 |
| 根工程配置 | Integration Owner | CLI-01 合并窗口已关闭；当前未出租 | 跨线需求先登记，不得顺手修改 |

## 6. Review 与修复记录要求

每个任务 handoff 至少记录：

- Owner 自审结果和专项测试证据。
- 独立 Reviewer 按 Authorization、Idempotency、Transactions、Migrations、Observability、Backward Compatibility、Secrets、Failure Modes 八项给出的结论。
- 每条 finding 的严重级别、文件位置、复现方式、预期行为、修复提交和回归测试。
- 原 Reviewer 的复审结论；若发现新问题则继续 `FIXING -> RE_REVIEW`。
- 最终 `pnpm check` 结果、迁移/契约/客户端专项证据和合并提交。

## 7. 合并门状态

| 合并门 | 状态 | 当前证据/阻塞 |
|---|---|---|
| G0 版本控制与治理 | PARTIAL | `origin/main` 与历史 CI/镜像发布证据存在，本地 CODEOWNERS 和镜像工作流 push-`main` 资格门已建立；当前候选仍缺新远端运行，服务端 required checks、审批、CODEOWNER enforcement、force-push/删除保护未闭合。本轮 GitHub 实时读取受本地代理连接失败阻断；见 [G0 远程证据](../../.handoffs/G0-REMOTE-EVIDENCE.md) |
| G1 工程与契约基础 | PASSED | FND-01、FND-02、INF-01、DAT-01 已完成 |
| G2 模块公共接口 | PASSED | CLI-03 独立 Review 两轮关闭 1 项 P2，复审零问题；全部模块均已通过 |
| G3 应用组合 | EVIDENCE_BLOCKED | 仓库侧生产组合与门禁见 `G3-PRODUCTION-COMPOSITION.md`：API/Worker 正式 Factory、独立数据库角色、COS/ClamAV Provider、Task Outbox/Inbox/投影、AMQPS 重试/DLQ、告警/恢复声明和镜像迁移制品验证已实现。真实首发策略、COS Bucket、镜像摘要、TLS/CAM/告警/恢复证据及消费者显式启用必须由受保护生产发布闭合；缺少已审合同的 Notification/Workflow/File Job 不得臆造 |
| G4 Walking Skeleton | PASSED_LOCAL | 当前树的业务中立组合验收已通过：浏览器登录、Workbench Registry/Deep Link、Form UI/服务端提交、稳定 FileReference、Task API、Outbox/RabbitMQ/隔离 Worker/Inbox、Audit/Trace 及第二 Session 的 Task/Notification 耐久观察均闭合；三类授权拒绝、依赖故障、重试和相同 HTTP 重放已验证，权威清单第 17 节 17/17 已完成，combined gate 独占产生 `mainWalkingSkeletonReady=true`。该状态不代表预发布、生产或 G5 签收 |
| G5 第一阶段完成 | EXTERNAL_BLOCKED | G4 本地业务中立验收已通过；OPS-01 预发布发布/回滚、OPS-02 真实恢复与安全演练、真实日志/Sentry/COS/主机证据及正式签收仍未闭合，详见 `.handoffs/G5-FIRST-STAGE-SIGNOFF.md` |

## 8. 验收证据索引

验收证据以任务 handoff、自动化测试输出、迁移清单、生成制品校验、E2E 报告、部署/恢复报告和最终 `pnpm check` 为准。本表只保存链接与状态，不复制敏感日志、请求正文、个人数据、Secret 或 Provider Payload。

- PLT-01：见 [任务 handoff](../../.handoffs/PLT-01.md)，Owner `pnpm check` 140/140、Audit PostgreSQL 3/3、App Registry PostgreSQL 4/4；独立 Review 六项 finding 全部关闭且复审零问题；合并提交 `0c816d3`。
- INT-01：见 [任务 handoff](../../.handoffs/INT-01.md)，25/25 模块测试、`pnpm check` 140/140、contracts 28/28；独立 Review 五项 finding 全部关闭且复审零问题；合并提交 `843c6b7`。
- PRC-02：见 [任务 handoff](../../.handoffs/PRC-02.md)，35/35 模块测试、PostgreSQL 3/3、`pnpm check` 140/140；两轮独立 Review 的 UTC 日历、规范化指纹、租约接管、Source 结果校验和外部 Schema 泄漏问题均关闭；合并提交 `c39fce6`。
- CLI-01：见 [任务 handoff](../../.handoffs/CLI-01.md)，23/23 客户端测试、生产构建与 Bundle 门、`pnpm check` 140/140；独立 Review 和 1366/1440/1920/390 四视口 Browser 验收零问题，状态路由及恢复动作通过且 Console 零错误/警告；合并提交 `e4d76e7`。
- 共享 Lockfile 对账：Integration Owner 在 CLI-01 合并后发现已合并 Workspace importer 未进入 Lockfile，使用离线非 frozen 生成窗口重建后，再以 frozen lockfile 验证；提交 `7a696f5`。该修复不改变任何工作包依赖声明。
- AIG-01：见 [任务 handoff](../../.handoffs/AIG-01.md)，Round 1 四项与 Round 2 两项 finding 全部关闭；Round 3 对 `2f20bb1` 零问题，AI Gateway 14/14、Contracts 28/28、`pnpm check` 140/140；acceptance `0d8f7f8`，合并提交 `119b94c`；Integration Owner 更新 Lockfile 后 frozen install 与主线 140/140 通过，Lockfile 提交 `36e77fe`。
- PLT-02：见 [任务 handoff](../../.handoffs/PLT-02.md)，Form unit 9/9、PostgreSQL 2/2，Configuration unit 9/9、PostgreSQL 4/4，Contracts 28/28、主线 `pnpm check` 140/140；Round 1 四项 finding 全部关闭，Round 2 零问题；合并提交 `f2447ad`，Lockfile 窗口提交 `41cbeb0`。
- PRC-03：见 [任务 handoff](../../.handoffs/PRC-03.md)，三轮 Review 后 finding 为零；PostgreSQL 3/3、普通测试 18/18、`pnpm check` 140/140；G2 evidence `ce0ce85`，合并提交 `055c4ac`。
- PLT-03：见 [任务 handoff](../../.handoffs/PLT-03.md)，Round 1 的扫描读取上限、隔离路径逃逸、隔离部分完成、对账破坏协调中间态和上传截止时间 TOCTOU 五项 finding 全部关闭；Round 2 新 finding 为零；单元/契约/Adapter 25/25、PostgreSQL 5/5、Contracts 28/28、`pnpm check` 140/140；G2 evidence `7543a73`，合并提交 `4e5ffd3`。
- CLI-02：见 [任务 handoff](../../.handoffs/CLI-02.md)。初始 G2 后在主线清洁依赖环境连续暴露 Bundle Gate 固定入口、HTML 属性边界/canonical realpath 去重、React 18/19 类型污染和 NoticeBar 适配契约放宽问题，均由原 Reviewer复查关闭；最终实现 `7444570`，G2 restoration `cc305c0`，合并提交 `c1526ab`。Clean/frozen 证据：typecheck、Node 5/5、Vitest 27/27、生产构建、Bundle 540942/614400、主线 `pnpm check` 140/140；actionable finding 与未决架构/契约均为零。
- CLI-02 declaration portability follow-up：六个 Taro 配置改用公开类型边界，独立 Review finding 为零；合并提交 `3a853c0`。CLI-03 共享依赖图下 clean offline frozen install、Node 6/6、Vitest 27/27、Bundle 540932/614400 与主线 `pnpm check` 140/140 通过，G2 restoration 恢复。
- CLI-03：见 [任务 handoff](../../.handoffs/CLI-03.md)。业务中立 External Portal 产出独立 H5/weapp 制品，只消费空的 external allowlist，生产失败关闭；H5 Cookie 与 weapp 不透明句柄边界分离。Integration Review 关闭 Taro 声明可移植性和离线 bootstrap 两项 P2；Independent Review Round 1 关闭 external-client 源码守卫模块语法覆盖的一项 P2，Round 2 复审零问题。Node 4/4、Vitest 17/17、H5 543010/665600、weapp 596220/2097152 与主线 `pnpm check` 140/140 通过；合并提交 `87666f6`，G2 Review 证据已闭合。
- OPS-01：见 [任务 handoff](../../.handoffs/OPS-01.md)，三轮 Review 后 finding 为零；Release tests 10/10、`pnpm compose:check`、真实 Docker Edge 集成和 `pnpm check` 140/140 通过。Edge 以 non-root/read-only 运行；Release Gate 绑定 evidence URI 与 SHA-256；Secret 文件使用专用 reader GID 和 0440 权限。G2 evidence `568f513`，合并提交 `23cc467`。
- E2E-01：见 [当前环境证据](../../tests/e2e/CURRENT-ENVIRONMENT-EVIDENCE.md)。提交 `a7c3e90` 的组合验收输出 `e2e-browser-to-worker-causal-evidence-passed`，第 17 节 17/17 闭合，三类 workforce/permission 拒绝、依赖故障恢复和相同 HTTP 重放通过；G4 状态为 `PASSED_LOCAL`，不外推为预发布、生产或 G5 签收。完整 `pnpm check` 为 133/133 Turbo tasks。
- OPS-01 当前复核：见 [当前发布门核验](../../.handoffs/CURRENT-OPS-01-VERIFICATION.md)。发布、镜像、迁移和生产配置专项 34/34，两个基础 Compose 与 previous-key overlay 合成渲染 4/4，Worker drain 数值门通过；Docker Engine 已恢复并支撑本地组合 E2E，但这些结果仍不能升级为真实 Registry 镜像、预发布发布或回滚证据。
- OPS-02/G5：仓库恢复证据门 13/13 通过，示例 Manifest 可重复校验且对缺失、复用、敏感值和自报布尔失败关闭。当前权威清单实际为 201 项；第 17 节四个旧部分项闭合后，审计结果为 166 项仓库级已验证、24 项部分完成、11 项外部阻塞。真实外部恢复和安全演练仍被环境与治理前置条件阻塞；状态与签收证据矩阵见 [G5 签收状态](../../.handoffs/G5-FIRST-STAGE-SIGNOFF.md)。
- CMP-API-DB-READY：见 [API 生产组合 handoff](../../.handoffs/API-PROD-COMPOSITION.md)。API 在迁移兼容检查后使用公共 `DatabaseRuntime.healthCheck()` 维护有界 Readiness 缓存；超时、拒绝和不可用均失败关闭，恢复后可重新就绪。独立 Review 关闭超时后底层查询可能重叠的 P2。该条是历史子包证据，最终 G3 状态以后述汇总交接为准。
- ADR-0025/0026：项目负责人于 2026-07-28 接受两项决策，见 [授权决策 handoff](../../.handoffs/AUTH-POLICY-DECISION.md) 与 [异步运行决策 handoff](../../.handoffs/ASYNC-RUNTIME-DECISION.md)。接受不产生 Permission/Role/Grant seed，也不授权启用生产消费者。
- AUTH-PERSIST-01：见 [任务 handoff](../../.handoffs/AUTH-PERSIST-01.md)。迁移 `0000000012`、不可变策略版本/发布、原子当前指针、事务发布和耐久裁决记录已实现；后续独立 Review 关闭未知合同版本和 denied-decision policy authority 两项问题；Authorization 34/34，隔离 PostgreSQL 17.5 集成 5/5。
- ASYNC-RABBIT-ADAPTER-01：见 [任务 handoff](../../.handoffs/ASYNC-RABBIT-ADAPTER-01.md)。固定 `amqplib@2.0.1` 的文件式 AMQPS Adapter 已覆盖 Confirm/Return、背压、ACK/NACK、固定 TTL 重试、DLQ、流控、Readiness 和有界 Drain/Close；后续独立 Review 关闭 NACK 后同 message ID 重试误取旧 Confirm 的 P1，Rabbit 专项 20/20。该条是 Adapter 历史子包证据；生产组合和密封策略已在 G3 汇总闭环，消费者仍因外部激活证据保持禁用。
- CMP-API-AUTH-PERSIST：见 [任务 handoff](../../.handoffs/CMP-API-AUTH-PERSIST.md)。API 已通过公共入口组合 PostgreSQL Authorization Store/Recorder、Organization workforce 只读解析、PostgreSQL Audit 与认证审计 Adapter；无完整当前策略时保持 Not Ready。后续独立 Review 证明通用 DB 健康不能证明 Audit repository 可写，authentication-audit required Readiness 在 audit-owned 非写健康合同出现前保持 Not Ready。API 164 passed/5 skipped。
- FILE-STORAGE-CONFORMANCE：见 [任务 handoff](../../.handoffs/FILE-STORAGE-CONFORMANCE.md)。供应商中立的私有 `StorageAdapter` 契约门已由 Local Adapter 执行，覆盖 grant 输入/安全输出、可信 inspect、bounded read、delete/quarantine 幂等收敛与失败分类；File Center 29/29。真实 COS Adapter 仍须使用受审测试 Bucket 跑同一门，不得以 Local 结果冒充生产证据。
- OPS-G3-GATES：见 [任务 handoff](../../.handoffs/OPS-G3-GATES.md)。迁移制品完整性、Worker Drain `< stop_grace_period` 与 BFF previous-key overlay 静态门已实现并通过；这些局部门不等同于 G3 通过。
- G3-PRODUCTION-COMPOSITION：见 [汇总 handoff](../../.handoffs/G3-PRODUCTION-COMPOSITION.md)。仓库侧生产组合和发布门已随 `e090dda` 合并；`pnpm check` 140/140、PostgreSQL 隔离集成 39/39、API 170、Worker 114、Authorization 53。当前状态为 `EVIDENCE_BLOCKED`，只等待受保护环境中的真实策略首发、COS、不可变镜像、RabbitMQ TLS/CAM、告警和恢复/Drain 证据以及显式消费者启用。
- G0-REMOTE-EVIDENCE：见 [治理/制品证据 handoff](../../.handoffs/G0-REMOTE-EVIDENCE.md)。TLS/API 读取已恢复且 GitHub 配置的默认分支为 `main`；本地 CODEOWNERS 和镜像工作流 push-`main` 资格门已补齐，但空远程没有可保护的分支、PR 或 CI，公开 ruleset 导出为空，required checks/独立 PR review/force-push/删除保护证据仍未闭合。尚无受信 Registry Digest 和成功发布记录。
- CMP-HTTP-PLATFORM：见 [任务 handoff](../../.handoffs/CMP-HTTP-PLATFORM.md)。Application Registry、Form Schema、File Center 新增 7 个 internal-only HTTP operation、6 个业务中立权限及生成客户端；上传重放、错误映射和表单 256 KiB/深度 32/节点 10000 上限经独立复审关闭 3 项 P2，复审无新增 finding。
- CMP-API-PLATFORM-HTTP：见 Registry、Form、File 三份任务 handoff。三条框架无关 Adapter 与 Nest Controller 已接入：静态权限和模块当前授权分层执行，File mutation 在服务前校验幂等/Origin/CSRF，Form 原始字节限额在授权前执行，入站 W3C Trace 贯穿授权记录与模块响应。该条是 HTTP 历史子包证据；生产查询与 COS/ClamAV Provider 已在 G3 汇总组合，真实 COS conformance 仍需外部环境证据。
- CMP-WORKER-GENERIC：见 [任务 handoff](../../.handoffs/CMP-WORKER-GENERIC.md)。Worker 已组合只读迁移兼容、DB 健康缓存和双账户 AMQPS 资源生命周期；部分获取泄漏、关闭重入和迁移目录漂移经独立复审关闭 1 项 P1 与 2 项 P2。Task policy 不可用时稳定非零退出，未声明 topology、未调用 `consume`、未注册 handler。
- G3 合并前集成门：独立 Review 的 2 项 P1 与 2 项 P2 已关闭；供应商中立 StorageAdapter 契约门已加入。该历史快照的测试数字已由 `G3-PRODUCTION-COMPOSITION` 最终证据取代。
- 共享 Lockfile 当前窗口：Integration Owner 合并 CLI-02 Integration 修复和 PLT-03 后，使用离线 lockfile-only 窗口重建，再执行 frozen install 与主线 `pnpm check` 140/140；Lockfile 提交 `3c65dfa`。Taro `@tarojs/plugin-framework-react 4.2.1` 对 Vite 4 的 peer warning 保留为共享依赖评估项，未未经评审修改 Vite 版本。

# E2E-01 Environment Evidence

> 2026-08-02 follow-up: Docker Engine 29.6.2 was restored and the modified combined path was rerun successfully. The browser Task runner now composes the real Organization/Authorization services for linked, unlinked, inactive-employment, and permission-denied contexts; all three denials return `403`, and the accepted request is replayed over HTTP with the same Session, CSRF token, Trace, URL, and idempotency key.

- Date: 2026-08-02
- Scope: repository/local dependency readiness, a real browser/Workbench/Nginx/BFF/Keycloak authentication slice, a business-neutral in-process platform slice, an isolated 10-service process skeleton, a durable PostgreSQL + Flowable + RabbitMQ main slice, and a real File Center + ClamAV slice
- Result: the repository-local `17` Walking Skeleton path has executable combined evidence. The authenticated browser loads the composed Application Registry, resolves its Deep Link, renders and submits a published synthetic form, and completes a Task through the real BFF Session and CSRF boundary. The durable PostgreSQL/Flowable/RabbitMQ chain preserves the exact Trace and clean FileReference through Outbox, isolated Worker, Inbox, Audit, Task, and Notification projections. A second authenticated browser Session observes those durable projections before `mainWalkingSkeletonReady=true` is emitted.

Fresh local verification:

- `pnpm e2e:browser-auth:integration`: drives installed headless Chromium through Nginx and the Workbench origin into the real BFF Authorization Code + PKCE path and Keycloak login form using a runtime-created synthetic user. It verifies the allowlisted return path, a rotated HttpOnly/Secure/SameSite BFF Cookie, no browser-visible Keycloak Token, refresh rotation, rejection of a forged callback, wrong CSRF, the pre-login fixation Cookie, the rotated-out Cookie, and a clock-expired session. The synthetic Keycloak user, Chrome profile, containers, Volumes, network, and temporary Secrets are removed after the run.
- `pnpm e2e:main-chain:integration`: passed with `status=e2e-main-chain-durable-evidence-passed`, `durable=true`, a PostgreSQL-backed Form Schema release and submission reference, one stable synthetic `FileReference`, Task Center denied/dependency-failure/retry/replay coverage, completed real Flowable task and instance, two trace-correlated Outbox records, two Inbox receipts, 30 durable Audit records, TLS RabbitMQ Confirm/manual ACK, one Notification, and source version 2. The stable file fixture is synthetic and is not claimed as the output of the separate real File Center/ClamAV run. Failed and successful attempts removed containers, networks, Volumes, and temporary Secrets.
- `pnpm e2e:file-clamav:integration`: passed against `clamav/clamav:1.4.5-debian`; clean content became `available`, EICAR content became `quarantined`, duplicate clean/malicious scan operations replayed, and an unavailable scanner left content `pending_scan` with a retryable failure. The isolated container, network, and Volume were removed.
- `pnpm e2e:compose:integration`: passed the strengthened 10-service composition with `worker=isolated-rabbit-postgres`, a published Task Lifecycle CloudEvent, `projection=open:1`, and one durable Inbox receipt. The run also exposed and corrected the production/E2E RabbitMQ queue permission matrix before the successful rerun, then removed all isolated resources.
- `pnpm e2e:combined-evidence:integration`: passed on 2026-08-02 with `status=e2e-browser-to-worker-causal-evidence-passed`, `applicationRegistryLoaded=true`, `deepLinkResolved=true`, `deepLinkNavigated=true`, `formRendered=true`, `formServerValidated=true`, `formFileReferenceMatched=true`, `auditCorrelationVerified=true`, `durableTaskObserved=true`, `durableNotificationObserved=true`, `externalEvidence=true`, and `mainWalkingSkeletonReady=true`. The browser validation receipt is a mandatory main-chain input and is matched by Trace, Form digest/version, submission reference, and complete FileReference before durable persistence and Audit checks. The main durable slice itself emitted `mainWalkingSkeletonReady=false`; while its PostgreSQL container remained alive, a fresh BFF Session for the same restored synthetic Keycloak subject queried the Prisma Task/Notification stores directly. Only that final browser observation promoted the combined result. The temporary Keycloak dump and identity Secret fixture were mode `0600` files under the runner-owned temporary directory and were removed without logging their values.
- `pnpm e2e:rabbit-jobs:integration`: passed with `status=e2e-rabbit-jobs-passed`, `inboxDuplicates=2`, `notifications=1`, and `sourceVersion=2`. Two test-only Jobs traveled through real TLS RabbitMQ, Outbox Confirm publication, Worker consumption/manual ACK, and Eventing Inbox duplicate recognition; the isolated container, network, Volume, and temporary TLS/account directory were then removed.
- `pnpm e2e:flowable-workflow:integration`: passed with `status=e2e-flowable-workflow-passed`, completed real Flowable task and process instance, `sourceAuthorizations=1`, `sourceVersion=2`, and `workflowCompletionEvents=1`. The isolated PostgreSQL/Flowable containers, network, Volume, and temporary Secret directory were then removed.
- `pnpm compose:test:integration`: passed; all seven dependency services became healthy, then the isolated containers, networks, Volumes, and temporary Secret directory were removed.
- `node tests/e2e/environment-preflight.mjs`: passed with `composeScope=full-process-skeleton`, the exact 10-service set, `contractBlockers=[]`, no implementation gaps, and `mainWalkingSkeletonReady=false`. This static check intentionally cannot promote readiness; only the successful combined execution can do so.
- `node --test tests/e2e/environment-preflight.test.mjs`: 5/5 passed after adding fail-closed coverage for external evidence bridge drift and real isolated Worker drift.
- `pnpm auth:test:integration`: 181/181 passed, including the real Keycloak/Redis integration paths.
- `pnpm db:test:integration`: 40/40 passed.
- `node scripts/check/run-rabbitmq-integration.mjs`: 10/10 TLS, permission, Confirm/Return, ACK, and redelivery checks passed.
- Flowable 1/1, Eventing Outbox 6/6, Task Center 4/4, Notifications 3/3, and Audit 3/3 isolated integration tests passed.
- Seven PostgreSQL module integration runners (Organization, Authorization, App Registry, Audit, Business Configuration, Form Schema, and File Center) passed 29/29 tests after their direct-Docker cleanup was changed to remove attached anonymous Volumes.
- A before/after comparison of all dangling Docker Volume identifiers around those seven runs was identical (`139 -> 139`, no added or removed identifiers). Existing unrelated and historical dangling Volumes were observed but not deleted.
- Final `pnpm check`: 133/133 Turbo tasks passed. The command explicitly set `AI_CRM_AUTHORIZATION_REDIS_PASSWORD_FILE` to `deploy/compose/.runtime/dev/redis_password`; the development Redis service was healthy during the gate. No Secret value was copied into the command, logs, or repository evidence.
- Follow-up package results before the final repository gate included API 193 passed with 5 skipped, E2E 75/75, and Workbench 34/34. The final `pnpm check` completed with 133/133 Turbo tasks after the successful combined gate.

The combined main slice uses PostgreSQL-backed Form Schema, Task Center, Workflow Ledger, synthetic authoritative source, Eventing Outbox/Inbox, Notification, submission-reference, and Audit stores with real Flowable and TLS RabbitMQ. The top-level runner proves the authenticated browser Task POST, its bounded causal command, exact W3C Trace/FileReference equality, and durable submission, Outbox, Worker, Inbox, and Audit effects. The isolated process composition also proves the real Task Projection Consumer against PostgreSQL, TLS RabbitMQ, Eventing Inbox, and the Prisma Task store. The reviewed walking-skeleton gates are complete; production business modules remain out of scope.

## Executable evidence

| Area | Existing executable entry | What it proves | Main-chain limitation |
|---|---|---|---|
| Full process skeleton | `pnpm e2e:compose:integration` | Packages API, Worker, and Workbench with seven dependencies, publishes a Task Lifecycle event through the real isolated Worker, and proves durable Projection/Inbox evidence. | Workbench still does not drive the durable business-neutral flow through the Task API. |
| Dependency Compose | `pnpm compose:test:integration` | PostgreSQL, Redis, RabbitMQ, Keycloak, Flowable, ClamAV, and Nginx can become healthy in one isolated project. | The Compose definition contains no API, Worker, or client. Nginx returns a fixed local health response and `503` for other routes. |
| Keycloak and Redis | `pnpm auth:test:integration` | Real Keycloak Authorization Code + PKCE, JWKS verification, Redis session creation/rotation/invalidation, and logout with synthetic users. | It invokes API package integration tests, not a browser through a composed API/Nginx/Workbench process. |
| Browser authentication | `pnpm e2e:browser-auth:integration` and `pnpm e2e:combined-evidence:integration` | Installed Chromium traverses the Workbench/Nginx/BFF/Keycloak path with a runtime synthetic user; Cookie, Token-boundary, callback, CSRF, fixation, rotation, expiry, workforce/permission denial, Registry/Deep Link, Form submission, accepted completion, identical HTTP replay, and durable Task/Notification polling checks execute. | Closed for the local synthetic acceptance path; staging and production identity evidence remain external. |
| RabbitMQ | `pnpm e2e:rabbit-jobs:integration` and `node scripts/check/run-rabbitmq-integration.mjs` | Real RabbitMQ TLS, hostname validation, exact test-only publisher/consumer permissions, Confirm/Return, both reviewed Job routes, Worker manual ACK, Inbox duplicate delivery, retry topology, and cleanup. | Eventing/source/Notification stores are in memory, the route is test-only, and production Task activation remains disabled. |
| Flowable | `pnpm e2e:flowable-workflow:integration` and `pnpm --filter @ai-crm/platform-workflow test:integration` | Real synthetic BPMN deployment, process start/query/cancel, human-task actions, Task Center completion through the Workflow Facade, source reauthorization, duplicate suppression, and cleanup. | The E2E binding is not installed in the full API/Worker composition and uses memory Ledger/Task/source state. |
| API | `pnpm --filter @ai-crm/api test` and `start` after build | HTTP/BFF/platform adapters and the executable composition root are tested independently. | Production readiness deliberately fails for unavailable providers/adapters; `compose.test.yml` does not start the API. |
| Worker | `pnpm --filter @ai-crm/worker test`, `start` after build, and `pnpm e2e:compose:integration` | Bootstrap, drain, Rabbit adapter, Outbox publisher, and sealed Task Projection behavior are tested; the E2E composition installs production PostgreSQL/RabbitMQ resources and the real Task Projection Consumer. | Production activation remains disabled and no unapproved consumer or provider contract is inferred. |
| Durable Flowable/Rabbit/PostgreSQL slice | `pnpm e2e:main-chain:integration` and `pnpm e2e:combined-evidence:integration` | One versioned submission and stable external FileReference precede Task Center completion; denial, inactive release, dependency failure, retry, command replay, real Flowable, PostgreSQL Outbox/Inbox, TLS RabbitMQ, Worker effects, and durable Audit share the browser-originated W3C trace. | The standalone main-chain command remains synthetic by design; only the combined runner is the 17-16 acceptance path. |
| ClamAV | `pnpm e2e:file-clamav:integration` and `pnpm e2e:combined-evidence:integration` | File Center sends clean and EICAR synthetic content to a real ClamAV daemon, proves available/quarantine/replay/unavailable behavior, and feeds the exact authorized `cleanFileReference` through submission, Task, Worker, and Audit evidence. | Production storage/provider activation remains outside this test-scoped evidence. |

## Reviewed contracts and remaining implementation gaps

| Acceptance item | Reviewed contract | Remaining implementation gap |
|---|---|---|
| `09-05` | Task source type `tests.walking-skeleton` has one exact formal completion command. | Closed for the test-scoped path: the updated runner verifies session resolution, browser mutation validation, Organization/Authorization rejection scenarios, identical idempotent HTTP replay, stable failure responses, and Trace arrival in a Docker-backed run. |
| `17-01` | Existing Keycloak/BFF contracts and synthetic authentication fixtures. | Implemented by `pnpm e2e:browser-auth:integration`; no remaining item-specific implementation gap is reported. |
| `17-03` | Application Registry composition and reviewed Deep Link resolution contracts. | Closed for the local synthetic path: the authenticated Workbench loads the composed Registry, resolves the allowlisted Deep Link through BFF, and navigates to the stable Task route under one Trace. |
| `17-08` | Task/Notification query contracts and isolated BFF Session boundary. | Closed for the local synthetic path: a fresh authenticated browser Session polls PostgreSQL-backed Task and Notification projections and matches their stable source identifiers. |
| `17-09` | File Center contracts, Form Schema/Task Center public APIs, the durable submission-reference migration, and the real ClamAV conformance slice. | Closed for the local synthetic path: one browser execution renders the published UI Schema, receives server validation, preserves the exact clean FileReference, and completes the matching Task. |
| `17-16` | W3C propagation, Eventing headers, durable Audit, and safe telemetry contracts. | Closed by the combined run: the authenticated browser Task POST passes real BFF Session/CSRF validation, records a bounded causal command, and the durable Outbox/RabbitMQ/Worker/Inbox/Audit evidence matches its Trace. |
| `17-17` | Durable Audit contract and safe correlation fields. | Closed for repository-local evidence: the verifier requires correlated Form and Task Audit facts and rejects Cookie, Token, Secret, form/file content, request/response payload, and SQL parameter material. |

The causal API join is now verified by the combined E2E. The preflight returns `contractBlockers=[]`, no implementation gaps, and `mainWalkingSkeletonReady=false`; the passing combined gate alone returns `true`. Production activation and confirmed business modules remain outside this test-scoped evidence.

# UI Web 效果原型

本目录是独立的中文 Mock 效果预览，不属于主项目 workspace，不连接真实 API，也不承载生产业务逻辑。

```powershell
pnpm --dir ui-web dev
```

默认地址：`http://127.0.0.1:4178/`。使用 `?view=dashboard`、`mail`、`chat`、`finance` 可直接打开四个界面。

验证结论待体验后记录在 `NOTES.md`，原型确认后应删除或重写为正式实现。

# Shared AI Workflows

This directory is the single implementation location for repository-specific AI workflows. `AGENTS.md` must explicitly reference any workflow that agents are required to load.

Do not duplicate skill bodies under tool-specific directories.

